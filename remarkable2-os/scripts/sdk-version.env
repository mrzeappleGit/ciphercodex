# Pinned official reMarkable SDK for the rm2 target.
# Set after the device OS update completes so SDK and device match exactly.
# Known-listed versions: see https://developer.remarkable.com/links
SDK_URL=
SDK_OS_VERSION=
